version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.x
    commands:
      - echo Installing dependencies...

  build:
    commands:
      - echo Build started...
      - python app.py

  post_build:
    commands:
      - echo Build completed successfully!